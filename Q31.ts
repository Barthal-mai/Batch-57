let userNames: string[]=["admin","barthalmai","Ahsan","Anjum","Inderias"];
if(userNames.length===0 ){
    console.log("we need some users");
}else{
    userNames=[];
    console.log("All user has been remove",userNames.length);
}